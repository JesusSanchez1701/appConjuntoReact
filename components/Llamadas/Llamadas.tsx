import { AppRegistry, Text, View } from 'react-native';

function Llamadas() {
    return (
        <View>
            <Text>llamada</Text>
        </View>
    )

}

AppRegistry.registerComponent('component_llamadas', () => Llamadas);
